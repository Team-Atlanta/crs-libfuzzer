services:
  # Coverage reporter - periodically generates llvm-cov reports from corpus seeds
  coverage-reporter:
    build:
      context: .
      dockerfile: coverage-reporter.Dockerfile

  # Original fuzzer service
  fuzzer:
    build:
      context: .
      dockerfile: runner.Dockerfile
    privileged: true
    shm_size: 2g
